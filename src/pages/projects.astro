---
import BaseLayout from "../layouts/BaseLayout.astro";
import projects from "../data/projects.json"
import Project from "../components/Project.astro";

//name, description, techStack,githubRepo,imageUrl,liveDemo
---

<BaseLayout>
  <h1 class="p-4 text-center">Work in progress....</h1>
  <p class="p-4 text-center">Soon will be looking great :)</p>
    <div class="p-4   w-[100vw] grid flex flex-col items-center justify-center  gap-6 lg:grid-cols-2 lg:items-start lg:p-[30px]">
       {projects.map((project)=><Project name={project.name} description={project.description}
       techStack={project.techStack} githubRepo={project.githubRepo} imageUrl={project.imageUrl}
       liveDemo={project.liveDemo}/>)} 
    </div>
</BaseLayout>



